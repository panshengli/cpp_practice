#include <iostream>
#include <cmath>
using namespace std;

int main() {
	double a = floor((9.95 * pow(10, 1) + 0.5)) / pow(10, 1);
    double b = 10.0;
    std::cout << a<<std::endl;
	printf("%lf",a);
	return 0;
}
